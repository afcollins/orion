tests:
  - name: node-density
    metadata:
      platform: "{{ platform }}"
      clusterType: "{{ clusterType }}"
      masterNodesType: "{{ masterNodesType }}"
      masterNodesCount: "{{ masterNodesCount }}"
      workerNodesType: "{{ workerNodesType }}"
      workerNodesCount: "{{ workerNodesCount }}"
      benchmark.keyword: node-density
      wildcard:
        ocpVersion: "{{ version }}*"
      networkType: OVNKubernetes
      jobType: "{{ jobtype | default('periodic') }}"
      pullNumber: "{{ pull_number | default(0) }}"
      organization: "{{ organization | default('') }}"
      repository: "{{ repository | default('') }}"
      fips: "{{ fips | default('false') }}"
      not:
        stream: okd

    metrics:
      - name: ovnCPU-sbdb-${ag}
        metricName.keyword: containerCPU
        labels.namespace.keyword: openshift-ovn-kubernetes
        labels.container.keyword: sbdb
        metric_of_interest: value
        agg:
          agg_type: ${ag}
        fan_out:
          - ag: avg
          - ag: min
          - ag: max
        labels:
          - "[Jira: Networking / ovn-kubernetes]"
        direction: 1
        threshold: 10
      - name: ovnCPU-sbdb-pcntiles
        metricName.keyword: containerCPU
        labels.namespace.keyword: openshift-ovn-kubernetes
        labels.container.keyword: sbdb
        metric_of_interest: value
        agg:
          agg_type: percentiles
          percents: [10,25,50,75,90]
        labels:
          - "[Jira: Networking / ovn-kubernetes]"
        direction: 1
        threshold: 10
